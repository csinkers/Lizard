// Generated by LizardGenFs
using SerdesNet;
#nullable enable

namespace Lizard.Comms;

public sealed class Lizard1Serializer : ILizard1
{
    readonly PacketHandler _send;

    public Lizard1Serializer(PacketHandler send)
    {
        _send = send ?? throw new ArgumentNullException(nameof(send));
    }

    static MemoryWriterSerdes Header(Lizard1Message type)
    {
        MemoryWriterSerdes sw = new();
        sw.EnumU16(nameof(type), type);
        return sw;
    }

    public void Continue()
    {
        var sw = Header(Lizard1Message.Continue);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public LRegisters1 Break()
    {
        var sw = Header(Lizard1Message.Break);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public LRegisters1 StepIn()
    {
        var sw = Header(Lizard1Message.StepIn);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public LRegisters1 StepOver()
    {
        var sw = Header(Lizard1Message.StepOver);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public LRegisters1 StepOut()
    {
        var sw = Header(Lizard1Message.StepOut);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public LRegisters1 StepMultiple(uint cycles)
    {
        var sw = Header(Lizard1Message.StepMultiple);
        sw.UInt32(nameof(cycles), cycles);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public void RunToAddress(LAddress1 address)
    {
        var sw = Header(Lizard1Message.RunToAddress);
        LAddress1.Serdes(nameof(address), address, sw);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public LRegisters1 GetState()
    {
        var sw = Header(Lizard1Message.GetState);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        LRegisters1 result = LRegisters1.Serdes("result", null, sr);
        return result;
    }

    public uint GetMaxNonEmptyAddress(ushort seg)
    {
        var sw = Header(Lizard1Message.GetMaxNonEmptyAddress);
        sw.UInt16(nameof(seg), seg);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        uint result = sr.UInt32("result", 0);
        return result;
    }

    public List<LAddress1> SearchMemory(LAddress1 start, uint length, byte[] pattern, uint advance)
    {
        var sw = Header(Lizard1Message.SearchMemory);
        LAddress1.Serdes(nameof(start), start, sw);
        sw.UInt32(nameof(length), length);
        sw.Bytes32(nameof(pattern), pattern);
        sw.UInt32(nameof(advance), advance);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        List<LAddress1> result = sr.List32<LAddress1>("result", null, LAddress1.Serdes);
        return result;
    }

    public List<LAssemblyLine1> Disassemble(LAddress1 address, uint length)
    {
        var sw = Header(Lizard1Message.Disassemble);
        LAddress1.Serdes(nameof(address), address, sw);
        sw.UInt32(nameof(length), length);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        List<LAssemblyLine1> result = sr.List32<LAssemblyLine1>("result", null, LAssemblyLine1.Serdes);
        return result;
    }

    public byte[] GetMemory(LAddress1 address, uint length)
    {
        var sw = Header(Lizard1Message.GetMemory);
        LAddress1.Serdes(nameof(address), address, sw);
        sw.UInt32(nameof(length), length);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        byte[] result = sr.Bytes32("result", null);
        return result;
    }

    public void SetMemory(LAddress1 address, byte[] bytes)
    {
        var sw = Header(Lizard1Message.SetMemory);
        LAddress1.Serdes(nameof(address), address, sw);
        sw.Bytes32(nameof(bytes), bytes);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public List<LBreakpoint1> ListBreakpoints()
    {
        var sw = Header(Lizard1Message.ListBreakpoints);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        List<LBreakpoint1> result = sr.List32<LBreakpoint1>("result", null, LBreakpoint1.Serdes);
        return result;
    }

    public void SetBreakpoint(LBreakpoint1 breakpoint)
    {
        var sw = Header(Lizard1Message.SetBreakpoint);
        LBreakpoint1.Serdes(nameof(breakpoint), breakpoint, sw);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public void EnableBreakpoint(uint id, bool isEnabled)
    {
        var sw = Header(Lizard1Message.EnableBreakpoint);
        sw.UInt32(nameof(id), id);
        sw.Bool8(nameof(isEnabled), isEnabled);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public void DeleteBreakpoint(uint id)
    {
        var sw = Header(Lizard1Message.DeleteBreakpoint);
        sw.UInt32(nameof(id), id);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public void SetRegister(LRegister1 reg, uint value)
    {
        var sw = Header(Lizard1Message.SetRegister);
        sw.EnumU8(nameof(reg), reg);
        sw.UInt32(nameof(value), value);
        _ = new MemoryReaderSerdes(_send(sw.GetMemory()));
    }

    public List<LDescriptor1> GetGdt()
    {
        var sw = Header(Lizard1Message.GetGdt);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        List<LDescriptor1> result = sr.List32<LDescriptor1>("result", null, LDescriptor1.Serdes);
        return result;
    }

    public List<LDescriptor1> GetLdt()
    {
        var sw = Header(Lizard1Message.GetLdt);
        var sr = new MemoryReaderSerdes(_send(sw.GetMemory()));

        List<LDescriptor1> result = sr.List32<LDescriptor1>("result", null, LDescriptor1.Serdes);
        return result;
    }
}